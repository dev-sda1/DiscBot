//Rank.js
//Shows user their coins balance.

const { RichEmbed } = require("discord.js");

module.exports = {
    name: "rank",
    category: "fun",
    description: "Displays your server XP and rank.",
    run: async (client, message, args) => {
        //Currently blank - to be used when xp is added
    }
}